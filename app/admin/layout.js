import "@/app/globals.css";

export const metadata = {
  title: "Photo Manager | United Flooring",
  robots: "noindex, nofollow",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
